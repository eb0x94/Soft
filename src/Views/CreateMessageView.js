import React, { Component } from 'react';

export default class CreateMessageView extends Component{
    render(){
        return(
            <h1>Welcome to the create message view.</h1>
        );
    }
}
