import React, { Component } from 'react';

export default class CreateMessageView extends Component{
    render(){
        return(
            <div>
                Create message is under construction.
            </div>
        );
    }
}
